export const metadata = { title: '커버 이미지 생성기 — Suno YT Dashboard' };

export default function CoverImageGeneratorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
